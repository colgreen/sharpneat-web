# Lessons

| Date | Lesson |
| --- | --- |
| 2026-07-08 | In this Windows-hosted repo used from WSL, line-ending policy matters for clean Git status. `.gitattributes` now sets text working-tree files to CRLF while keeping normalized LF in the index. |

Add concise dated entries here when a task reveals another reusable pattern, pitfall, failed attempt, or workflow preference.
